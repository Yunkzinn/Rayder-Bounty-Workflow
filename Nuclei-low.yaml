vars:
  OUTPUT_HTTPX: "aliveSubs"
  OUTPUT_NUCLEI_LOW: "nuclei_low"
  
parallel: false
modules:
  - name: Nuclei Low
    cmds:
      - cat {{OUTPUT_HTTPX}} | nuclei -etags ssl,netlify -severity low -t ~/nuclei-templates/ -o {{OUTPUT_NUCLEI_LOW}} | notify
    silent: true
    